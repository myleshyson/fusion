# Main Instructions File

Testing the generation of command artifacts in Fusion.

Default Command
fusion

Only generates for cursor
fusion --cursor

Only generates for some custom path
fusion --path some/path/

Only generates skills for default agents
fusion --skills
Only generates mcp for default agents
fusion --mcp
Only generates guidelines for default agents
fusion --guidelines

Default options no interaction
fusion --no-interaction


